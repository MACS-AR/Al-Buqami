import { db, ref, get, child, showToast, auth, onAuthStateChanged } from './firebase.js';
const byId=id=>document.getElementById(id), money=v=>Number.isFinite(Number(v))?Number(v):0;
function render(invoice){const text=(id,v)=>{const e=byId(id);if(e)e.textContent=v};text('lbl-invoice-id',invoice.invoiceId||`INV-${invoice.orderId||'UNKNOWN'}`);const date=invoice.date?new Date(invoice.date):new Date();text('lbl-invoice-date',`التاريخ: ${Number.isNaN(date.getTime())?'غير محدد':date.toLocaleDateString('ar-SA')}`);text('lbl-cust-name',invoice.customerName||'عميل كريم');text('lbl-cust-phone',`جوال: ${invoice.customerPhone||'غير مسجل'}`);text('lbl-cust-address',`العنوان: ${invoice.address||'غير مسجل'}`);const status=byId('lbl-invoice-status');if(status){status.textContent=invoice.status||'بانتظار الدفع';status.style.backgroundColor=invoice.status==='مدفوعة'||invoice.status==='تم التوصيل'?'#2ecc71':'#e67e22'}const body=byId('invoice-items-body');body.replaceChildren();let subtotal=0;(Array.isArray(invoice.products)?invoice.products:[]).forEach(item=>{const qty=Math.max(1,Number(item.quantity)||1),unit=money(item.price),total=qty*unit;subtotal+=total;const tr=document.createElement('tr');tr.innerHTML=`<td style="padding:12px">${item.name||'منتج'}</td><td style="padding:12px;text-align:center">${qty}</td><td style="padding:12px;text-align:center">${unit.toFixed(2)} ر.س</td><td style="padding:12px;text-align:left">${total.toFixed(2)} ر.س</td>`;body.appendChild(tr)});const tax=subtotal*.15;text('invoice-subtotal',`${subtotal.toFixed(2)} ر.س`);text('invoice-tax',`${tax.toFixed(2)} ر.س`);text('invoice-grand-total',`${(subtotal+tax).toFixed(2)} ر.س`);const qr=byId('invoice-qrcode');if(qr&&window.QRCode){qr.replaceChildren();new QRCode(qr,{text:`Al-Buqami|${invoice.invoiceId||invoice.orderId||''}|${date.toISOString()}|${(subtotal+tax).toFixed(2)}`,width:120,height:120})}}
async function fetchInvoice(id,user){if(!id)return showToast('أدخل رقم الطلب','error');try{const s=await get(child(ref(db),`invoices/${id}`));if(!s.exists()||s.val().userId!==user.uid)throw new Error();render(s.val())}catch{showToast('الفاتورة غير موجودة أو لا تخص حسابك','error')}}
document.addEventListener('DOMContentLoaded',()=>{const input=byId('input-search-invoice'),button=byId('btn-fetch-invoice');onAuthStateChanged(auth,user=>{if(!user){showToast('سجل الدخول لعرض فواتيرك','error');setTimeout(()=>location.href='login.html?return=invoice.html',800);return}button?.addEventListener('click',()=>fetchInvoice(input?.value.trim(),user));const id=new URLSearchParams(location.search).get('orderId');if(id){if(input)input.value=id;fetchInvoice(id,user)}})});
